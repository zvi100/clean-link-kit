{
  "name": "@clean-link-kit/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@clean-link-kit/core": "0.1.0",
    "vite": "^6.1.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.300",
    "typescript": "^6.0.3"
  }
}
